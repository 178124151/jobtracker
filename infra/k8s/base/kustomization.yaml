apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - config.yaml
  - postgres.yaml
  - redis.yaml
  - backend.yaml
  - frontend.yaml
  - hpa.yaml
  - ingress.yaml

commonLabels:
  app: jobtracker
  managed-by: kustomize